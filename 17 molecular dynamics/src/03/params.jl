#= ==========================================================================================
=============================================================================================
params
=============================================================================================
========================================================================================== =#

# space
xlims = (-10, 10); # μm
dims = 3;

# fluid
viscosity = 1; # (μm)²/(μs)

# particles
radii = [1 1]; # μm
positions = [
    [[-5,0,0]]
    [[5,0,0]]
]; # μm
coupleForces = true;
coupleTorques = false;

# magnetic field
period = 10 # μs
magneticField(t) = 10*[sin(t * 2pi / period); 0; cos(t * 2pi / period)] # mT

# simulation
simulationTime = 10e3; # μs
ticksSaved = 10;
